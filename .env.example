# Core model keys (optional but recommended for best answer quality)
GEMINI_API_KEY=
GOOGLE_API_KEY=

# Shared auth token between OpenWebUI and the local API bridge
API_AUTH_KEY=openwebui-local-key

# Optional: set true only on trusted private network
DISABLE_API_AUTH=false
